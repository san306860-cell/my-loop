# 共享约定

> **竖切之间唯一的通道。** 每条竖切开工前读它，做完更新它。
> 有了它，一条竖切换一个子代理也不掉上下文；没有它，第二条竖切会把第一条做过的事重新发明一遍。
>
> **只写「猜错了两边就对不上」的东西。** 显而易见的不写 —— 这不是文档，是接头暗号。
> 没有跨竖切共享就删掉这个文件。

## 数据形状

（表 / 字段名 / 类型 / 谁是主键 / 哪些字段允许空）

## 接口约定

（路径命名 / 请求响应的包裹形状 / 分页参数长什么样）

## 错误码

| 码 | HTTP | 什么时候用 |
|---|---|---|
| | | |

## 命名与位置

（公共工具在哪 / 测试文件放哪 / 环境变量前缀）

## 已经踩定的坑

（上一条竖切发现、后面每条都要绕开的东西。够门槛的同时写进 DECISIONS.md）
