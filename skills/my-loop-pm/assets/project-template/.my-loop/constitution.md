# constitution · 本项目特有的施工约束

通用实现规矩（最简阶梯、测试与证据、回执格式）写在 `my-loop-worker` 一处，这里不重复。
任务范围与当前授权以项目 `AGENTS.md`、用户指令和派工单为准。

本文件只写**这个项目**才有的约束：包管理器、禁区目录、命名约定、必须走的命令、已知的环境坑。每条一行，写不出理由的不写。

- 启动 / 测试 / 验证命令：见项目 `AGENTS.md`
-

## 验收

验收方式由 PM 记录用户选择（`state.json.review_policy`），不能额外添加固定人审，也不能擅自省略用户指定的人审。
