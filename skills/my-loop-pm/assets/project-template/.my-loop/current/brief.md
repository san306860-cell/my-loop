# 技术简报（align 产物 · 人点头才算数）

> 一次变更一份。人没确认的简报不许进下一阶段；bug 走 Bug Brief 版式（见文末）。

**分级**: simple ／ medium ／ big ／ bug　　**人已确认分类**: ☐

**Goal**（一句话：用户干了什么、然后看到什么——不是「接口返回 200」）:

>

**Definition of Done**（必含一条用户可直接感知的运行条件，例如「一条命令能跑起来」）:

| # | 标准 | 拿什么验（可执行命令或操作） | 验收结论 |
|---|---|---|---|
| D1 | | | 过／没过／没法验 |
| D2 | | | |

**Boundary**（这次动哪里、不动哪里）:

**Constraints**（硬约束）:

**Known**（已确认的事实）:

**Unknowns**（未确认的——**重大的必须问人，不许当假设用**）:

**Decisions**（本次相关的已拍板 + 新拍板，新的同步写入 `decisions.jsonl`）:

**Non-goals**（这次明确不做，谁都不许顺手）:

## 反例表（这套设计在什么输入下会塌 · 每条由人当场拍）

| 类别 | 会怎么塌 | 人的裁决 | 落点 |
|---|---|---|---|
| 边界值 | | 挡住／接受 | 挡住→硬约束＋守卫；接受→已知限制 |
| 边界值 | | | |
| 边界值 | | | |
| 并发与重试（含「响应丢了客户端重试」） | | | |
| 并发与重试 | | | |
| 并发与重试 | | | |
| 失败路径 · 外部操作：<写文件／起停进程／调外部服务／连库／网络请求> | 失败或超时后停在什么终态、谁回滚 | | |
| 失败路径 · 外部操作： | | | |
| 失败路径 · 外部操作： | | | |

失败路径**按这次会碰的外部操作逐个列，一个不漏**（行数跟着操作数走，不凑三条也不封顶）。

**人点头（「理解对了」）**: ☐　　日期:

---

## Bug Brief 版式（bug 旁路用，替换上面各节）

现象 ／ 可靠复现命令 ／ 根因 ／ 修复边界 ／ 不可破坏行为 ／ 回归证明（修复前红＋修复后绿，两份真实输出）
分叉：local（直接修）／ structural（先问人）／ 其实是设计错了（升级走正常流程）
